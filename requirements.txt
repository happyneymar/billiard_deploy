# Django
Django>=6.0,<7.0

# 生产环境服务器（Railway/Vercel 等使用）
gunicorn>=23.0.0

# 数据库 URL 解析（用于云平台 PostgreSQL）
dj-database-url>=2.0.0
psycopg2-binary>=2.9.0

# 白噪音（用于在生产环境提供静态文件）
whitenoise>=6.6.0

# Cloudinary（用于 Render 等无持久磁盘环境的图片/视频上传存储）
cloudinary>=1.44.0
